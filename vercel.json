{
  "builds": [
    { "src": "server/server.js", "use": "@vercel/node" },
    { "src": "npm run build", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/.*", "dest": "server/server.js" },
    { "src": "/(.*)", "dest": "build/$1" }
  ]
}
